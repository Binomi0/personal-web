import styled from 'styled-components';

export default (theme) => ({
  biography: {
    // margin: '0 2rem',
    // textAlign: 'justify',
  },
  imagen: {
    minHeight: '200px',
    height: 'auto',
    width: '100vw',
    objectFit: 'cover',
  },
});

export const StyledSection = styled.section`
  margin: 2rem 0;
  padding: 1rem;
`;
