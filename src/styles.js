import styled from 'styled-components';

export const Content = styled.div`
  margin: 1rem;
  position: absolute;
  top: 0;
  left: 0;

  font-size: 0.9rem;

  div {
    margin-right: 1rem;
    font-size: 0.8rem;
    font-weight: bold;
  }
`;
